# My Experiment

This script implements 3 experiments:

1. Train on emotion + polarity → test emotion  
2. Train on polarity → test polarity  
3. Train on both → test polarity  

Uses:
- BERT, RoBERTa, DeBERTa
- Logistic Regression, SVM
- Multi-task learning